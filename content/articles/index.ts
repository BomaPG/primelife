import type { EducationArticle } from "@/lib/content/types";

/**
 * PRD Section 9 requires three reviewed articles: menopause, blood-pressure,
 * blood-sugar (F8-AC1). Bodies are a content-authoring step — left empty on
 * purpose for this infrastructure scaffold (see PRD Section 16 placeholder).
 */
export const educationArticles: EducationArticle[] = [];
