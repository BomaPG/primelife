import type { Affirmation } from "@/lib/content/types";

/**
 * Rotating affirmation list (PRD Section 16 placeholder). Left empty on
 * purpose for this infrastructure scaffold; `lib/daily` selects
 * deterministically by date once this list is populated.
 */
export const affirmations: Affirmation[] = [];
