import type { MealSet } from "@/lib/content/types";

/**
 * PRD Section 9 requires four meal sets: blood-pressure, blood-sugar,
 * heart-health, weight-management (F7-AC1). Meals are a content-authoring
 * step — left empty on purpose for this infrastructure scaffold (see PRD
 * Section 16 placeholder).
 */
export const mealSets: MealSet[] = [];
