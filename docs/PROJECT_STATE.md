# PrimeLife: project state

Tracks what exists, what's been verified, and what's next. Updated at the end of each prompt-pack step. See `PrimeLife-MVP-PRD.md` for the full spec and `SCOPE.md` for the binding scope summary.

## What's built

**Step 5 — Track and Insights** (this step):

- Track (`app/(tabs)/track/page.tsx`, `F6`) — the third tab, sibling to `home` and `move` under `(tabs)`. Fetches raw rows via the existing `dataAccess` range functions and hands them straight to the already-unit-tested pure functions in `lib/insights/derived` (`checkInStreak`, `hydrationStreak`, `movementStreak`, `getWeeklySummary`, `getTrends`) — this page adds no new derived-logic function, only fetching and rendering, per this step's explicit constraint. Renders three sections: Streaks (`F6-AC2`), "This week" (`F6-AC3`, same shape as Home's weekly summary, now including the walk distance Step 4 added), and Trends (`F6-AC4`) — water and walk-minutes as bar charts, sleep-quality as a plain three-line count list (a chart adds nothing over three numbers). A fourth section, Daily logs (`F6-AC1`), lists the last 7 local days newest-first, each showing water glasses, which meals were logged, sleep hours/quality, and walk count/minutes for that day — this is new UI-level grouping (filtering the fetched rows by date for display), not a derived-logic function, since Section 11 doesn't specify one for it.
- Streaks are computed over a wider 60-day range read (`STREAK_LOOKBACK_DAYS` in `track/page.tsx`), not the 7-day window the rest of the page uses — `checkInStreak`/`hydrationStreak`/`movementStreak` walk backward from today until the first gap with no upper bound, so feeding them only 7 days of rows would silently cap every streak at 7. `getWeeklySummary` and `getTrends` still receive the same wider arrays for water/walks/check-ins (harmless — both filter internally to their own 7-day window via `lastNDates`), so this doesn't cost a second read of those tables.
- `components/insights/WeekBarChart.tsx` — a small reusable 7-bar CSS chart (no charting dependency, per PRD Section 12's data-thrift guidance), used for both the water and walk-minutes trends. Bar height is relative to the week's own max (not a fixed scale), with a 4% floor so a zero day still renders a visible sliver rather than nothing; the full week's values are also exposed via `aria-label` on the chart container so the data isn't sight-only. Labels use `text-lg` throughout — the codebase's established C4 floor — rather than a smaller Tailwind size that would fall under 18px.
- `lib/content/mealTypeOptions.ts` (`MEAL_TYPE_OPTIONS`) — extracted from `MealsQuickLog.tsx`, which had this exact `{type, label}` array inline; Track's daily-logs view needed the same mapping to list which meals were logged each day, and duplicating the literal array a second time risked the two views' labels drifting apart. Both call sites now import the one array.
- Home (`app/(tabs)/home/page.tsx`) — added a "See streaks and trends" link under "This week", the same contextual-entry-point pattern Step 4 used for Move (no shared tab bar exists yet, so each tab is reached by a link from where it's contextually relevant rather than global nav).

**Step 4 — Walking Journal**:

- Move (`app/(tabs)/move/page.tsx`, `F5`) — the second tab, sibling to `home` under `(tabs)`. Owns today's walks + a week-scoped `getWeeklySummary` call in its own `reload()`, same pattern as Home. Renders the log-walk form, a list of today's walks (multiple per day, per the explicit constraint), and "This week" totals for walks/minutes/distance (`F5-AC2`). A static "Exercise library coming soon" line sits beneath the journal, per PRD Section 6's note that the Move tab may show one such line while that feature is deferred.
- `components/WalkLogForm.tsx` (`F5-AC1`) — required duration (minutes), optional distance (km) and note. Submit is disabled unless duration is a positive number and distance (if entered) is a non-negative number — mirrors the validate-before-write convention already used by `PreferencesFields`'s water target and `SleepQuickLog`'s hours field, rather than silently dropping bad input. Resets after each successful log so a second walk the same day is one more fill-and-submit, no extra navigation (multiple walks per day, no dedup key on `walks`).
- `lib/insights/derived.ts` — `WeeklySummary` gained `totalWalkDistanceKm` (sum of `distanceKm ?? 0` over the week's walks), alongside the `totalWalks`/`totalWalkMinutes` Step 3 already computed. Move calls the same `getWeeklySummary` Home uses, passing only the week's walks and empty arrays for the other categories, rather than introducing a second summary function — one pure function, two call sites each reading the fields they need.
- Home (`app/(tabs)/home/page.tsx`) — the Movement goal line now links to `/move` ("Log a walk"), and the "This week" walks line shows distance alongside minutes. No changes to Home's data-fetching: it already read `getWalks`/`getWalksInRange` into its own `reload()` in Step 3, so a walk logged on Move and a client-side navigation back to Home is enough to show it live — `F5-AC3` needed no new wiring on the Home side.

**Step 3 — Home, Daily Check-In, quick logs**:

- Home (`app/(tabs)/home/page.tsx`, `F3`) — replaces the Step 2 placeholder with the real dashboard. Owns all of today's + this week's data in one `reload()` that every write handler calls after its write, so quick-log changes reflect immediately with no page reload (F3-AC3). Renders: today's tip (`F3-AC1`), a check-in launcher or "Checked in today ✓", today's goals with live progress (`F3-AC2`, via `getTodaysGoals`), the three quick-log controls, a weekly summary (`F3-AC4`, via `getWeeklySummary`), and the daily affirmation (`F3-AC5`) — each read from local Dexie state only, no network call (`F3-AC6`).
- Daily Check-In (`components/CheckInModal.tsx`, `F2`) — one question at a time; tapping an answer immediately advances and auto-submits after the third, so completing it is exactly three taps with no separate submit tap (F2-AC5). Auto-launches from Home the first time a given local day is opened, unless already checked in or already dismissed that day (`F2-AC1`); dismissing doesn't relaunch it again the same day but leaves it reachable via a "Daily check-in" button (`F2-AC3`). Writes through the existing `completeCheckIn` (`F2-AC2`, `F2-AC4` — the streak itself is unchanged, already tested in Step 1).
- `lib/checkInDismissal.ts` (+ test) — small localStorage helper backing the "dismissed today" state. Deliberately outside Dexie/the data-access layer: it's UI session state, not app data, and the PRD fixes the Dexie schema exactly with no new fields allowed.
- Quick-log components (`components/quickLogs/{Water,Meals,Sleep}QuickLog.tsx`, `F4`) — presentational only; Home owns the data-access calls. Water is tap-to-increment/decrement (`F4-AC1`). Meals mark breakfast/lunch/dinner done with an optional note (`F4-AC2`). Sleep sets optional hours (on blur) and quality (on tap) via the same `upsertSleepLog` the check-in uses for quality — both key off `sleepLogs`' `date` primary key, so there is structurally one row per day regardless of which path writes to it (`F4-AC3`, and this step's explicit no-duplicate-rows constraint). No calorie/macro/weight field exists anywhere (`F4-AC4`).

## What's verified

- `npx tsc --noEmit`, `npx eslint .` — zero errors.
- `npm run test` (Vitest) — 49/49 tests pass. No new test file this step: Track adds no derived-logic function of its own (per this step's explicit constraint), so there's nothing new for the existing `lib/insights/derived.test.ts` to cover — the streaks/summary/trends math it exercises is unchanged.
- `npm run build` (`next build --webpack`) — succeeds; eight static routes now (`/track` added to Step 4's seven).
- `/code-review` (medium effort) on the diff: one finding — Track's daily-logs view duplicated the exact `{type, label}` meal-type array already defined inline in `MealsQuickLog.tsx`, risking the two views' labels drifting apart. Fixed by extracting `lib/content/mealTypeOptions.ts` (`MEAL_TYPE_OPTIONS`) and importing it from both call sites, following the same pattern as the existing `lib/content/topicOptions.ts`.
- **QA note**: `CLAUDE.md` names gstack's `/browse`/`/qa` skills for this step, but neither is installed in this session (only a fixed built-in skill set was available). Substituted the closest available tooling — the `run` skill's browser-driven pattern, driving the pre-installed headless Chromium directly via Playwright — against the real production build (`next build` + `next start`), not the dev server, so this exercises the same service-worker precache path a real offline user would.
- **Step 5, F6-AC1 through AC5**, driven live against the production build:
  - Seeded one day of real activity — completed the check-in (sleep good, feeling great, moved yesterday yes), added water to exactly the default target (8/8 glasses), marked breakfast done, logged one walk (25 min, 3.0 km) — then opened Track via Home's new "See streaks and trends" link.
  - **F6-AC2, streaks**: with exactly one day of qualifying activity, "Check-in streak: 1 day", "Hydration streak: 1 day", "Movement streak: 1 day" all read correctly — confirming the streak functions receive the wider 60-day range read (a single day of history correctly produces a streak of 1, not 0 or a value skewed by the read window).
  - **F6-AC3, weekly summary**: "Average water: 1.1 glasses/day" (8÷7), "Nights with sleep logged: 1", "Walks: 1 (25 min, 3.0 km)", "Meals logged: 1", "Check-ins: 1", "Most common feeling: great" — all matched the single day of seeded activity exactly.
  - **F6-AC4, trends**: both bar charts' `aria-label`s (read directly via `getAttribute`, not just eyeballed) enumerated all 7 days with today correctly showing "8 glasses" / "25 min" and every other day "0" — confirmed the chart isn't just visually plausible but carries the real per-day data. Sleep-quality distribution read "Good: 1, Okay: 0, Poor: 0".
  - **F6-AC1, daily logs**: today's entry (first, newest-first, labeled "(Today)") read "Water: 8 / 8 glasses", "Meals: Breakfast ✓ · Lunch · Dinner", "Sleep: good", "Walks: 1 walk, 25 min" — all six other days in the window correctly showed the zero/not-logged state, not stale or repeated data from today.
  - **F6-AC5, offline**: separate fresh profile, seeded 3/8 glasses of water and dismissed the check-in (deliberately *not* hitting the hydration goal or checking in, to verify zero-state rendering too, not just the happy path), visited Track once online to precache the route, confirmed `navigator.serviceWorker` `"activated"`, then went fully offline and reloaded. Streaks correctly read 0/0/0 (no check-in, water under target), the weekly summary and daily-logs water figures matched the offline-seeded data exactly, and — checked directly via Playwright's `request` event log, not just visual inspection — **zero non-origin network requests fired while offline**, confirming F6-AC5 isn't just "the page didn't crash" but genuinely zero-network.
- Full flow driven live against the production build (Steps 3–4, re-verified still passing):
  - Fresh onboarding → check-in auto-launches on first Home visit (F2-AC1); three taps (one per question, no submit tap) completes it and closes the modal (F2-AC5).
  - **Check-in-to-logs, verified by reading IndexedDB directly, not just the UI**: completing the check-in with sleep quality "Good" immediately showed "Good" pressed in the Sleep quick-log; then setting hours via the quick-log and reading `sleepLogs` straight from IndexedDB showed exactly one row — `{date, hours: 7.5, quality: "good", ...}` — confirming the check-in and the quick-log wrote through the same seam into the same row, not two.
  - Dismiss flow (`F2-AC3`) tested end to end on a separate fresh profile: "Not now" closes the modal; reloading Home afterward does **not** relaunch it; the manual "Daily check-in" button reopens it; completing it from there writes exactly one `checkins` row (verified via IndexedDB — `getAll()` returned a single entry).
  - Water quick-log: four taps of "+" against a water target of 4 showed live "4 / 4 glasses — goal reached" with no reload; "−" is disabled at zero.
  - Meals quick-log: marking breakfast done and setting a note, then reading `mealLogs` from IndexedDB directly, showed exactly one row (`{mealType: "breakfast", done: true, note: "Akara and pap"}`) and Home's goal counter read "1 / 3 logged".
  - Weekly summary numbers (`nightsWithSleepLog`, `checkInsCompleted`, `totalWalks`, etc.) matched the day's activity in the rendered text.
  - **Offline (`C1`), explicit per this step's instructions**: loaded Home once to let the service worker precache, confirmed `navigator.serviceWorker.ready` reports `"activated"`, then killed the origin server outright (not just simulated) and reloaded — Home rendered full state (goals, weekly summary, check-in status) with zero console errors, entirely from precache + local IndexedDB. Then, still with the server down, tapped "+" on the water quick-log twice — the write succeeded and the UI updated live, confirming IndexedDB reads *and writes* work with zero network, not just cached reads.
  - **Step 4, F5-AC1**: logged a walk with duration only (20 min, both optional fields blank) — appeared in "Today's walks" as "20 min". Logged a second walk the same day with duration 35, distance 2.5 km, and a note — appeared as its own row ("35 min · 2.5 km" + note text), confirming multiple walks per day render as separate rows, not an overwrite.
  - **Step 4, negative-distance guard**: typed duration 10 / distance −3 into the form; the "Log walk" button stayed disabled the whole time (`isDisabled` checked directly), so the bad value was never submitted, let alone silently dropped.
  - **Step 4, F5-AC2**: after the two walks above, Move's "This week" section read "Walks: 2", "Minutes: 55", "Distance: 2.5 km" — matches (20+35) minutes and the one walk that had a distance.
  - **Step 4, F5-AC3, verified by client-side navigation, not a reload**: from a fresh Home showing "No walk yet", clicked through to Move, logged a walk, then clicked "Back to Home" (an in-app `Link`, not `location.reload()`) — Home immediately read "Walk logged today" and "Walks: 2 (55 min, 2.5 km)" in the weekly summary, sourced from Home's own `reload()` running on remount, not from any state passed between the two pages.
  - **Step 4, F5-AC4, offline**: fresh onboarding, visited Move once online (so its route precaches), confirmed `navigator.serviceWorker` reports `"activated"`, then set the browser context fully offline and reloaded Move — the form and empty-state text rendered from precache with zero console errors. Logged a walk (12 min, 0.8 km) while still offline — the write succeeded and "Today's walks" / "This week" updated live; navigating to Home (still offline) showed "Walk logged today" and "Walks: 1 (12 min, 0.8 km)", confirming the offline write reached IndexedDB and both screens read it back correctly with zero network.
- Accessibility spot-check on the rebuilt Home: computed `font-size` on the `h1` is 33.75px (`text-3xl` × the root bump), computed `min-height` on buttons is 49.5px — both comfortably clear the `C4` floors. Reviewed a mobile (375×812) screenshot: legible, well-spaced, the selected sleep-quality button is visually distinct by fill/weight, not color alone. Move's screenshots (375×812) show the same pattern: 44px+ tap targets, no color-only state (the walk rows and week stats are plain text/borders). Track's bar-chart labels deliberately use `text-lg`, not a smaller Tailwind size, to stay above the `C4` 18px floor even for secondary chart labels — confirmed in the mobile screenshot that the day-of-week and value labels read at the same size as the rest of the page's body text, not shrunk.

Two QA-tooling notes, not app bugs:
- `/browse`'s `click @ref` occasionally failed with "Selector matched multiple elements" on repeated snapshots of the check-in modal; worked around with a direct `document.querySelectorAll(...)[i].click()`. Didn't affect what was being verified (the app's own behavior), just how the click was dispatched.
- The Playwright driver hit the same class of issue clicking the check-in modal's sleep-quality options: `button:has-text("Good")` matched both the modal's own option and the (still-mounted-underneath) Sleep quick-log's "Good" button on Home, since the modal renders as an overlay rather than replacing the page. Scoping the click to `[role="dialog"] button:has-text(...)` resolved it.

Not yet verified: `content/tips/index.ts` and `content/affirmations/index.ts` are still empty (per PRD Section 0 rule 4 — placeholder content must be left as a placeholder, not authored by an implementing agent), so the tip/affirmation sections never actually render during this QA pass; they're conditionally hidden when empty rather than showing fabricated copy. The deterministic-selection mechanism itself (`getDayIndex`/`pickDailyTip`/`pickAffirmation`) is unit-tested and wired into Home correctly, but F3-AC1 and F3-AC5 can't be visually confirmed until that content is authored.

## What's next

**Step 6 — Nourish (`F7`):**

- A new Nourish tab (`app/(tabs)/nourish/page.tsx`), fourth sibling under `(tabs)`.
- The `MealSet`/`Meal` content shape and typed loaders (`getAllMealSets`, `getMealSetByTopic`) already exist (`lib/content/types.ts`, `lib/content/loaders.ts`) but `content/meals/index.ts` is still an empty `mealSets: MealSet[] = []` placeholder, same as `content/tips`/`content/affirmations` — per PRD Section 0 rule 4, an implementing agent must not author that placeholder content itself, so Nourish will render correctly-structured empty/loading states until the four meal sets (blood pressure, blood sugar, heart health, weight management — `F7-AC1`) are authored separately.
- Each category's breakfast/lunch/dinner grouping and each meal's "why this works" line come straight from `MealSet.meals` (`F7-AC2`) — no new content logic to write, only rendering.
- `F7-AC3`: blood pressure and blood sugar categories link to their matching education article — `getEducationArticleByTopic(topicId)` already exists for this, though `content/articles/index.ts` is equally still empty pending authoring.
- `F7-AC4`: persistent "not medical advice" disclaimer (`C5`) on every Nourish screen — `MealSet.disclaimer` is already part of the content shape for this.
- `F7-AC6`: attaching a shown meal to today's meal log writes `mealLogs.recommendedMealId` — `upsertMealLog` in `lib/db/dataAccess.ts` already accepts `recommendedMealId` (added in Step 3's schema, unused until now), so this is a new call site, not a new data-access method.
- `F7-AC5` (offline, no network) falls out for free the same way every other tab has: content is bundled at build time, not fetched.
